apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment01
  labels:
    app: test
spec:
  replicas: 3
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      name: my-pod
      labels:
        app: test
    spec:
      containers:
        - name: test-container
          image: nginx

